# Copy to .env. Used by sqlx-cli (`sqlx migrate run`) and by sqlx's compile-time
# query macros during `cargo build`. The .env file itself is gitignored.

DATABASE_URL=postgres://kengram:kengram@localhost:5432/kengram
